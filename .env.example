PORT=8080
APP_ENV=local

# Server Configuration (optional - defaults provided)
# SERVER_READ_TIMEOUT=15s        # Request read timeout
# SERVER_WRITE_TIMEOUT=60s       # Response write timeout (important for large files)
# SERVER_IDLE_TIMEOUT=120s       # Keep-alive timeout

# Database Configuration
BLUEPRINT_DB_HOST=psql_bp
BLUEPRINT_DB_PORT=5432
BLUEPRINT_DB_DATABASE=blueprint
BLUEPRINT_DB_USERNAME=melkey
BLUEPRINT_DB_PASSWORD=password1234
BLUEPRINT_DB_SCHEMA=public

# S3/MinIO Configuration
S3_ENDPOINT=minio:9000                    # Internal endpoint (for API server inside Docker)
S3_PUBLIC_ENDPOINT=localhost:9000         # Public endpoint (for presigned URLs outside Docker)
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin123
S3_BUCKET_NAME=uploads
S3_USE_SSL=false